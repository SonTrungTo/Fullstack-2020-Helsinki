const initialState = 'This app information is controlled by a store named Redux';

const reducer = (state = initialState, action) => {
    switch (action.type) {
    default:
        return state;
    }
};

export default reducer;